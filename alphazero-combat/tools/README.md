# Tools

`python -m src.azcombat.cli --seed <seed> --output <new-dir>` wraps the existing generated-scenario runner and defaults to `--write-inputs-only`. Add `--execute` only when deliberately invoking the existing unattended game launcher.
