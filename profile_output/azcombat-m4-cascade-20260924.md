# M4 Cascade/Prepared 原生搜索速率诊断（2026-09-24）

目标：在不改模型参数、不更改搜索访问次数或晋级门槛的前提下，解释真实多层选择夹具为何未达到逐决策 100 sims/s。输入是 `artifacts/alphazero/m4-regression-20260924/cascade-paired-006/` 的 4 条完整 NativeWorker 导出及其 `decisionMetrics`。每条决策的有效速率按 `simulations / (elapsedMilliseconds / 1000)` 重算，选择根由 `choiceLayer > 0` 判定。

| Seed | 策略 | 普通根数 / 速率最小 / 中位 (sims/s) | 选择根数 / 速率最小 / 中位 (sims/s) | 最慢根的动作数、访问与层号 |
| --- | --- | --- | --- | --- |
| AZ-M4-NESTED-601 | baseline | 24 / 55.5 / 547.6 | 12 / 86.8 / 491.6 | 3 动作、56 次、第 0 层 |
| AZ-M4-NESTED-601 | candidate | 8 / 52.9 / 609.2 | 0 | 3 动作、54 次、第 0 层 |
| AZ-M4-NESTED-602 | baseline | 24 / 43.6 / 713.8 | 16 / 50.9 / 535.2 | 3 动作、44 次、第 0 层 |
| AZ-M4-NESTED-602 | candidate | 7 / 45.4 / 511.9 | 0 | 3 动作、48 次、第 0 层 |

最慢根均为第一次普通决策；紧随其后的决策也较慢，而同一进程后续中位数超过 490/s。首次搜索的 JIT/缓存预热或初始复杂状态是合理假设，**尚无逐函数 CPU 采样证明**，不能据此绕过逐决策门槛。候选的两条轨迹均真实死亡，且没有进入选择根；baseline 的 602 种子有真实第二层选择，但该轨迹的首次决策和首次选择均低于门槛。因此完整配对尚不合格。

分析方法：直接重算保存的原生 JSONL 指标与动作集合大小；本机未找到 `dotnet-trace`、`dotnet-counters` 或 PerfView 命令。没有在紧循环中插桩，也没有修改采样、访问次数或模型。下一步优先用进程级采样或低开销阶段计时拆分首个根的 Restore、Apply、模拟结算及运行时预热；优化后必须重新完整发布并以未见 seed 重测，不应只在已经预热的中位数上报告达标。

## 性能分析文件变更清单

| 文件 | 类型 | 用途 | 行 |
| --- | --- | --- | --- |
| `profile_output/azcombat-m4-cascade-20260924.md` | 新建 | 保存这次诊断与待验证假设 | 全文 |

没有为这次性能分析加入源码插桩；本报告可在完成诊断后单独删除。
